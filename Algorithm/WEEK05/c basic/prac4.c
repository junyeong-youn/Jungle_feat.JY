#include <stdio.h>
#include <time.h>

int main(void)
{
    srand(imte(NULL));
    int answer;
    int treatment = rand() % 4;
    int cnt = 0;
    int pre_cnt = 0;

    for (int i = 1; i =<3; i++)
    {
        int bottle[4] = {};
        do{
            cnt = rand() % 2 + 2;
        }while(cnt = pre_cnt);

        int include = 0;
        printf("%d 번째 시도", i);
        

    }





    print("-----------------------------------------------------------")
    return 0;
}